#!/usr/bin/expect -f

set timeout 5

# Start the TUI application
spawn stack exec terminal-config-manager

# Give the application time to start
sleep 2

# Quit the application
send "q"

# Wait for application to exit
expect eof

puts "✓ SUCCESS: Application started and exited cleanly"
exit 0 